import 'package:flutter/material.dart';

class LIsteLocataire extends StatelessWidget {
  const LIsteLocataire({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "liste locatire",
        style: TextStyle(color: Colors.black),
      ),
    );
  }
}
